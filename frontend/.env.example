# Copy this file to .env.local for local development
# On Vercel, set VITE_API_URL in the Environment Variables dashboard

# Your Railway backend URL (e.g. https://ipotrack-production.up.railway.app)
# Leave empty for local dev — Vite proxy handles /api → localhost:3000
VITE_API_URL=
