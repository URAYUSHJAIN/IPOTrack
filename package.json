{
  "name": "ipotrack",
  "version": "1.0.0",
  "description": "Real-time Indian IPO Tracker — React + Node.js full-stack application",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm start --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm start --prefix backend",
    "install-all": "npm install && npm install --prefix frontend && npm install --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "keywords": [
    "ipo",
    "india",
    "stock-market",
    "gmp",
    "tracker",
    "react",
    "express",
    "puppeteer"
  ],
  "author": "IPOTrack Contributors",
  "license": "MIT"
}
